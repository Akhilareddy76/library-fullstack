# ==========================
# PORT (REQUIRED FOR RENDER)
# ==========================
server.port=${PORT:8080}

# ==========================
# DATABASE (PostgreSQL - Render)
# ==========================
spring.datasource.url=jdbc:postgresql://dpg-d4bl46h5pdvs7387g3ug-a.singapore-postgres.render.com:5432/library_db_jra0
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================
# CACHING
# ==========================
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=100,expireAfterWrite=30m

# ==========================
# SESSION SETTINGS
# ==========================
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=strict

# ==========================
# MAIL SETTINGS (SECURED)
# ==========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
